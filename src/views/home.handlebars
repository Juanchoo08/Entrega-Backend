<!doctype html>
<html>
    <head>
        <meta charset="utf-8"><title>Home - Productos</title>
    </head>
    <body>
        <div class="container">
            <h1>Productos</h1>
            <ul>
                {{#each products}}
                    <li>ID: {{this.id}} — <strong>{{this.title}}</strong> — ${{this.price}}</li>
                {{/each}}
            </ul>
            <a href="/realtimeproducts">Ir a Realtime Products</a>
        </div>
    </body>
</html>
