<div class="container">
    <h1>Carrito {{cart._id}}</h1>
    <ul>
    {{#each cart.products}}
        <li>
        <strong>{{this.product.title}}</strong> - Cantidad: {{this.quantity}}
        </li>
    {{/each}}
    </ul>
</div>
